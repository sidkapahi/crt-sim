## Prompt
for the how to guide can you leave the run locally thing out and put it in the dev stuff? this is meant to be a section on how to use the website

## Summary
Removed the "Get it running" (clone + local server) step from the README's
**How-To Guide** so that section is purely about using the app. Renumbered the
remaining steps (Load a clip → Tune the look → Frame it → Export, now 1–4).

The clone/serve instructions already exist in `docs/DEVELOPMENT.md` under
**Running locally**, so nothing needed to be added there.

Key file: `README.md`.
